"""\nEERF Report Generator Lambda\n\nGenerates a Markdown daily report from diff results and snapshot data.\nThe report includes Summary, Service Details, Approval Status, and Changes Detail sections.\n\nInput (event):\n    {\n        "bucket": "eerf-audit-bucket",\n        "diff_key": "diffs/2024-01-15.json",\n        "snapshot_key": "snapshots/2024-01-15.json"\n    }\n\nOutput:\n    {\n        "report_key": "reports/2024-01-15.md",\n        "diff_summary": { "new": N, "changed": N, "deleted": N, "unchanged": N, "excluded": N }\n    }\n\nRequirements: 3.1, 3.2, 3.3, 3.4, 3.5, 3.6\n"""\nimport json\nimport logging\nimport os\nfrom typing import Any, Dict, List, Optional\n\nimport boto3\n\nfrom approval_state import load_from_s3 as load_approval_from_s3\n\nlogger = logging.getLogger(__name__)\n\n# Initialize S3 client (overridden in tests)\ns3_client = boto3.client("s3")\n\n\ndef _extract_date_from_key(key: str) -> str:\n    filename = key.rsplit("/", 1)[-1]\n    date_str = filename.rsplit(".", 1)[0]\n    return date_str\n\n\ndef _compute_readiness_counts(services: List[Dict[str, Any]]) -> Dict[str, int]:\n    counts = {"ready": 0, "not_ready": 0, "error": 0}\n    for service in services:\n        readiness = service.get("readiness")\n        if readiness is None or not isinstance(readiness, dict):\n            counts["not_ready"] += 1\n            continue\n        recommendation = readiness.get("recommendation", "not_ready")\n        if recommendation == "ready":\n            counts["ready"] += 1\n        elif recommendation == "error":\n            counts["error"] += 1\n        else:\n            counts["not_ready"] += 1\n    return counts\n\n\ndef _compute_approval_counts(approval_data: Dict[str, Any], changes: List[Dict[str, Any]]) -> Dict[str, int]:\n    counts = {"approved": 0, "deferred": 0, "pending_approval": 0}\n    approval_services = approval_data.get("services", {}) if approval_data else {}\n    for change in changes:\n        category = change.get("category", "")\n        if category in ("Deleted", "Excluded"):\n            continue\n        account_id = str(change.get("account_id", ""))\n        fqdn = str(change.get("fqdn", ""))\n        composite_key = f"{account_id}:{fqdn}"\n        record = approval_services.get(composite_key, {})\n        status = record.get("status", "") if record else ""\n        if status == "Approved":\n            counts["approved"] += 1\n        elif status == "Deferred":\n            counts["deferred"] += 1\n        else:\n            counts["pending_approval"] += 1\n    return counts\n\n\ndef _build_service_lookup(snapshot: Dict[str, Any]) -> Dict[str, Dict[str, Any]]:\n    lookup = {}\n    for service in snapshot.get("services", []):\n        key = f"{service.get('account_id', '')}:{service.get('fqdn', '')}"\n        lookup[key] = service\n    return lookup\n\n\ndef generate_report(diff: Dict[str, Any], snapshot: Dict[str, Any], approval_data: Dict[str, Any], date_str: str) -> str:\n    summary = diff.get("summary", {})\n    changes = diff.get("changes", [])\n    services = snapshot.get("services", [])\n    service_lookup = _build_service_lookup(snapshot)\n    readiness_counts = _compute_readiness_counts(services)\n    approval_counts = _compute_approval_counts(approval_data, changes)\n\n    lines: List[str] = []\n    lines.append(f"[EERF \uc810\uac80 \ubcf4\uace0\uc11c]")\n    lines.append(f"\uc810\uac80\uc2dc\uac04: {date_str} (KST)")\n    lines.append("")\n\n    new_count = summary.get('new', 0)\n    changed_count = summary.get('changed', 0)\n    deleted_count = summary.get('deleted', 0)\n    failover_count = summary.get('failover', 0)\n    has_changes = new_count > 0 or changed_count > 0 or deleted_count > 0\n\n    excluded_count = summary.get('excluded', 0)\n    total_discovered = len(services)\n    protectable = max(total_discovered - excluded_count, 1)\n    coverage = round((approval_counts['approved'] / protectable) * 100, 1)\n\n    lines.append("1. \ubcf4\ud638 \ud604\ud669")\n    lines.append("")\n    lines.append("| \uad6c\ubd84 | \uc218\ub7c9 |")\n    lines.append("|------|------|")\n    lines.append(f"| \uc804\uccb4 \ubc1c\uacac | {total_discovered} |")\n    lines.append(f"| \uad00\ub9ac \uc911 | {approval_counts['approved']} |")\n    lines.append(f"| \ubbf8\uad00\ub9ac | {approval_counts['pending_approval']} |")\n    lines.append(f"| \uc81c\uc678 | {excluded_count} |")\n    lines.append(f"| \uad6c\uc131 \ubbf8\uc644 | {readiness_counts['not_ready']} |")\n    lines.append(f"| Failover \uc911 | {failover_count} |")\n    lines.append(f"| \uc2a4\uce94 \uc624\ub958 | {readiness_counts['error']} |")\n    lines.append(f"| \ubcf4\ud638 \ucee4\ubc84\ub9ac\uc9c0 | {coverage}% |")\n    if has_changes:\n        lines.append("")\n        lines.append("  \ubcc0\uacbd \ub0b4\uc5ed")\n        lines.append("")\n        lines.append("| \ubd84\ub958 | \uc218\ub7c9 |")\n        lines.append("|------|------|")\n        if new_count > 0:\n            lines.append(f"| \uc2e0\uaddc | {new_count} |")\n        if changed_count > 0:\n            lines.append(f"| \ubcc0\uacbd | {changed_count} |")\n        if deleted_count > 0:\n            lines.append(f"| \uc0ad\uc81c | {deleted_count} |")\n    lines.append("")\n\n    lines.append("2. \uc11c\ube44\uc2a4 \uc0c1\uc138")\n    lines.append("")\n    notable_changes = [c for c in changes if c.get("category") != "Unchanged"]\n    unchanged_changes = [c for c in changes if c.get("category") == "Unchanged"]\n    if notable_changes or len(unchanged_changes) <= 10:\n        lines.append("| FQDN | \uacc4\uc815 | Provider | \uc900\ube44\ub3c4 | \ubd84\ub958 |")\n        lines.append("|------|---------|----------|-----------|----------|")\n        for change in changes:\n            account_id = str(change.get("account_id", ""))\n            fqdn = str(change.get("fqdn", ""))\n            category = change.get("category", "")\n            composite_key = f"{account_id}:{fqdn}"\n            service = service_lookup.get(composite_key)\n            if service:\n                provider = service.get("edge_provider", "\u2014")\n                readiness = service.get("readiness", {})\n                readiness_str = readiness.get("recommendation", "\u2014") if isinstance(readiness, dict) else "\u2014"\n            else:\n                provider = "\u2014"\n                readiness_str = "\u2014"\n            lines.append(f"| {fqdn} | {account_id} | {provider} | {readiness_str} | {category} |")\n    else:\n        lines.append(f"\uc804\uccb4 {len(changes)}\uac1c \uc11c\ube44\uc2a4 \ubcc0\uacbd \uc5c6\uc74c.")\n    lines.append("")\n\n    lines.append("3. \uad6c\uc131 \ubcc0\uacbd \uc774\ub825")\n    lines.append("")\n    changed_entries = [c for c in changes if c.get("category") == "Changed" and c.get("details")]\n    if not changed_entries:\n        lines.append("N/A")\n    else:\n        for change in changed_entries:\n            fqdn = str(change.get("fqdn", ""))\n            details = change.get("details", {})\n            changed_fields = details.get("changed_fields", [])\n            before = details.get("before", {})\n            after = details.get("after", {})\n            lines.append(f"### {fqdn}")\n            lines.append("")\n            for field in changed_fields:\n                before_val = before.get(field, "\u2014")\n                after_val = after.get(field, "\u2014")\n                lines.append(f"- `{field}`: {before_val} \u2192 {after_val}")\n            lines.append("")\n    return "\n".join(lines)\n